%h2.content-subhead= pluralize @school.programs.count, t('activerecord.models.program')
- grid do
  - column width: 1, of: 2 do
    ! &nbsp;
  - column width: 1, of: 2, class: 'action-button' do
    = primary_button t('modules.actions.programs.new'), new_program_path, icon: :plus

%hr
= table_for @programs, columns: [:title, :departments]
