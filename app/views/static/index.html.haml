.pure-g#layout
  .introduction.pure-u-1-2.pad
    %header
      %h1
        %span.logo
          ProLearning
      %h2 Classroom Observation Tool
      %p
        Professional Learning is at its core a commitment to using our own strengths as a staff to help each and everyone of us to find great satisfaction in teaching by being as good a teacher as we can be.
  .signup.pure-u-1-2.pad
    - if user_signed_in? 
      %p Hello User
    - else
      %h3 Already Registered? Sign in
      = errors_for resource
      = form_for resource, as: resource_name, url: session_path(resource_name), html: {class: 'pure-form pure-form-aligned'} do |f|
        %fieldset
          .pure-control-group
            = f.label :email, 'Your Email'
            = f.email_field :email, autofocus: true, placeholder: 'Email', required: true
          .pure-control-group
            = f.label :password, 'Password'
            = f.password_field :password
          - if devise_mapping.rememberable?
            .pure-controls
              %label{for: "user_remember_me", class: "pure-checkbox signup"}
                = f.check_box :remember_me
                Remember Me
              %button.pure-button.pure-button-primary.pure-button-small{type: "submit"} Sign In
      %h3 Want to Get Started?
      %a.pure-button.pure-button-large.pure-button-primary{href: "#{new_registration_path(resource_name)}"} Register Your School
